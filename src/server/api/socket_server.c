#include "server.h"

/*
 * 기능:
 * - 실제 소켓 생성, bind, listen, accept, 클라이언트 입출력을 담당할 파일이다.
 *
 * 반환값:
 * - 각 함수는 추후 성공/실패 상태를 int로 반환하도록 설계한다.
 *
 * 흐름:
 * - 서버 시작 시 listen fd를 연다.
 * - accept된 연결에서 raw request를 읽는다.
 * - 읽은 요청을 작업 큐에 넣는다.
 * - 응답 문자열을 다시 클라이언트에 쓴다.
 *
 * 현재 상태:
 * - 아키텍처 분리를 위한 자리만 만든 스텁 파일이다.
 */
